# Developer Guide

## Run `example` directory

```
cd example
npm run build
# open index.html in browser
```

## Deploy to npm

```
npm version {major,minor,patch}
npm publish
git push
git push --tags
```
